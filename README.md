## 📦 Examples

**No Config**

```javascript
require("replup").config();
```

**With Config**

```javascript
require("replup").config({
  port: 8080,
  path: "/",
  message: "Hello!",
  debug: true,
});
```